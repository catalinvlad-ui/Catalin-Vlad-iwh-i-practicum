doctype html
html
  head
    meta(charset="utf-8")
    meta(name="viewport", content="width=device-width, initial-scale=1")
    title= title
    link(rel="stylesheet", href="/css/style.css")
  body
    header
      h1= title
      a.button(href="/update-cobj") Add to this table
    if records && records.length
      table
        thead
          tr
            each prop in properties
              th= prop
        tbody
          each r in records
            tr
              each prop in properties
                td= r.properties && r.properties[prop]
    else
      p No records yet.
    footer
      p Made for the Integrating With HubSpot I: Foundations Practicum.
